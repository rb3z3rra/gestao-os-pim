// Exporta configuracoes de ambiente para o app.
export const environment = {
  // Indica se o build e de producao.
  production: false,
  // URL base do backend usado pelas chamadas HTTP (vazio para usar o proxy).
  apiUrl: '',
};
